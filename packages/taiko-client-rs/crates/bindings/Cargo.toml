[package]
name = "bindings"
version = "0.1.0"
edition = "2024"

[dependencies]
alloy = { version = "1.0.36", features = ["sol-types", "contract"] }
serde = { version = "1.0", features = ["derive"] }

[lib]
doctest = false

[lints]
rust.warnings = "allow"
