\documentclass[a4paper,11pt]{article}
\usepackage[margin=2.55cm,bottom=2.8cm]{geometry}
\usepackage[T1]{fontenc}
\usepackage{mathpazo}
\usepackage{microtype}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{xltabular}
\usepackage{tikz}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage[hidelinks,bookmarks=true]{hyperref}
\usepackage{newunicodechar}

\newunicodechar{≤}{\ensuremath{\leq}}
\newunicodechar{≥}{\ensuremath{\geq}}
\newunicodechar{≠}{\ensuremath{\neq}}
\newunicodechar{→}{\ensuremath{\rightarrow}}
\newunicodechar{×}{\ensuremath{\times}}
\newunicodechar{Δ}{\ensuremath{\Delta}}
\newunicodechar{φ}{\ensuremath{\varphi}}
\newunicodechar{≈}{\ensuremath{\approx}}
\newunicodechar{−}{\ensuremath{-}}
\newunicodechar{—}{\textemdash}
\newunicodechar{–}{\textendash}
\newunicodechar{✓}{\ensuremath{\checkmark}}

% \nolinkurl keeps identifiers monospaced while allowing line breaks at
% underscores and punctuation; long consensus names must not escape tables.
\newcommand{\code}[1]{\nolinkurl{#1}}
\newcommand{\must}{\textsc{must}}
\newcommand{\mustnot}{\textsc{must not}}
\newcommand{\should}{\textsc{should}}

\linespread{1.055}
\setlength{\parskip}{0.38em}
\setlength{\parindent}{0pt}
\setlength{\emergencystretch}{2em}
\setlist{itemsep=0.22em,parsep=0.12em,topsep=0.4em,leftmargin=1.45em}
\titleformat{\section}{\normalfont\large\bfseries}{\thesection}{0.7em}{}
\titleformat{\subsection}{\normalfont\normalsize\bfseries}{\thesubsection}{0.6em}{}
\titlespacing*{\section}{0pt}{1.65em}{0.65em}
\titlespacing*{\subsection}{0pt}{1.15em}{0.45em}

\pagestyle{fancy}\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\fancyhead[L]{\footnotesize\itshape Slot-Chain v2}
\fancyhead[R]{\footnotesize Production Design Candidate}
\fancyfoot[C]{\footnotesize\thepage}

\newenvironment{designnote}
  {\par\smallskip\begin{list}{}{\leftmargin=1.0em\rightmargin=0.4em
      \parsep=0.25em\itemsep=0pt\topsep=0.25em}\item[]\small\itshape}
  {\end{list}\smallskip}

\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,calc}
\tikzset{
  box/.style={draw,rounded corners=1pt,align=center,font=\scriptsize,
              minimum height=8mm,inner sep=4pt},
  shaded/.style={box,fill=gray!14},
  arrow/.style={-{Latex[length=1.8mm]},line width=0.55pt},
  dasharrow/.style={arrow,dashed},
}

\begin{document}

\title{\vspace{-1.2cm}\bfseries Slot-Chain: A Preconfirmation Protocol\\[0.2em]
\large\mdseries Production Design Candidate --- v2.2}
\author{}
\date{August 2026}
\maketitle
\thispagestyle{fancy}

\begin{abstract}
Slot-Chain assigns one-second L2 slots to bonded builders.  Builders sign
parent-linked blocks off chain; any party can prove and submit a batch to L1.
Normal candidates compete for a fixed settlement interval.  After an objective
finality breach, or when a forced L1 message becomes due, anyone can prove an
unsigned deterministic escape block.  Recovery is an episode of renewable,
objectively expiring rounds, so a prover outage cannot make its target stale
forever.  This revision also fixes schedule sealing, candidate anchors, forced
resource bounds and ordering, immutable data sessions, the verifier ABI and
atomic migration.  It is an implementation candidate, not a production
authorization: the proving, gas, economics and adversarial gates in
\S\ref{sec:gates} remain mandatory.
\end{abstract}

\tableofcontents
\clearpage

\section{Status, claims and exclusions}\label{sec:status}

Normative words \must, \mustnot and \should have their usual RFC meaning.
Where prose and either executable model disagree, deployment is blocked until
the disagreement is resolved; neither source silently overrides the other.

\subsection{Claims}

Subject to L1 safety/liveness and a sound validity-proof system, the protocol
claims:

\begin{enumerate}
\item \textbf{Finalized-state safety.} L1 accepts only a proved state transition
  extending the stored canonical tuple.  Builder signatures never substitute
  for execution validity.
\item \textbf{Bounded protocol recovery.} A builder cartel, an absent
  aggregator, an empty builder registry, and missing standbys cannot prevent an
  unsigned escape candidate from advancing the canonical state.
\item \textbf{Forced transaction inclusion.} A correctly prepaid L1 envelope
  at the queue head is consumed by the next canonical block after it becomes
  due; it is executed if valid and consumed-and-discarded if invalid.
\item \textbf{Permissionless landing and recovery.} No allowlist, builder key,
  aggregator seat or payment recipient is required to submit a proof or an
  escape candidate.
\item \textbf{Deterministic lookahead.} Every implementation derives the same
  schedule from an authenticated finalized snapshot and the byte encoding in
  \S\ref{sec:schedule}.
\end{enumerate}

\subsection{Non-claims}

\begin{itemize}
\item A preconfirmation is not finality or insurance.  Before normal close it
  may be revoked by builder equivocation, an unprovable skip, a withheld body,
  or an escape commit.  Applications must price this as probabilistic/economic
  assurance and publish their own exposure limit.
\item A bond cannot cap aggregate off-chain promises: the protocol has no
  reservation ledger and a builder can sign mutually exclusive promises.
  \code{L_LEASE} is deterrence and a recovery source, not guaranteed coverage.
\item Protocol liveness is a capability claim.  Economic liveness additionally
  assumes at least one actor can fund proof generation and an L1 transaction.
  A payment failure never invalidates an otherwise valid canonical transition.
\item Censorship by L1 for longer than \code{T_INCLUDE_MAX}, failure of L1
  finality, or a broken proof system is outside the model.
\end{itemize}

\section{Actors and trust model}\label{sec:trust}

\begin{description}[style=nextline]
\item[Builder.] A registered address eligible for scheduled slots.  Builders
may equivocate, skip parents, withhold bodies, collude, or disappear.
\item[Aggregator.] The current auction seat, expected to collect data, prove and
submit normal candidates.  It has no exclusive consensus authority.
\item[Prover/lander.] Any address.  It supplies a validity proof and names an
optional reward beneficiary in the proof statement.  Proof validity, not
identity, authorizes transition.
\item[Forced-message sender.] Any L1 account that submits a gas-bounded,
prepaid L2 transaction envelope.
\item[L1.] Ethereum provides canonical ordering, timestamps, EIP-4788 beacon
roots, EIP-2935 historical block hashes, blob commitments and the KZG
point-evaluation precompile.
\end{description}

The safety threshold contains no honest-builder assumption.  Honest builders
improve normal throughput and preconfirmation reliability; the unsigned escape
lane provides state and forced-queue progress when all of them collude.
The L1-liveness assumption includes transaction inclusion and at least 64
canonical L1 blocks becoming available within \code{T_DEPTH_MAX=900 s}; a
longer L1 stall suspends, rather than violates, the L2 recovery clock.

\section{Clock domains and canonical state}\label{sec:state}

Before activation, Settlement is in \code{PREACTIVE}; candidate submission is
disabled and slot arithmetic saturates.  L2 slot zero is fixed by
\code{GENESIS_TIMESTAMP}, with
\begin{center}
\code{currentL2Slot = max(0, block.timestamp - GENESIS_TIMESTAMP)}.
\end{center}
L2 slot and every deadline are timestamp seconds.  L1 confirmation depth is
counted in L1 block numbers.  Beacon slot is used only in schedule
authentication.  Implementations \mustnot substitute one clock for another.

The L1 contract stores the following canonical tuple:

\begin{verbatim}
Canonical = (tipHash, tipSlot, stateRoot, messageCursor,
             canonicalizationBlock, winningDataCommitment)
\end{verbatim}

\code{canonicalizationBlock} is the L1 block that most recently committed the
tuple; no code attempts to read or store \code{blockhash(block.number)}, which
does not exist during that block.  Candidate acceptance compares the proved
base-tuple hash directly with current contract storage.  A candidate has one
prior-block anchor.  Normal anchors are authenticated by an EIP-2935 block hash,
the supplied execution header and MPT proofs.  A recovery round instead stores
\code{blockhash(block.number-1)} while available and requires that anchor to
reach depth \code{F_L1}.  An L1 reorg rolls the stored tuple and anchor back
together.  Initial values and bridge cursors are migration inputs in
\S\ref{sec:implementation}.

Every state-mutating external entry point begins with \code{sync()}.  If
\code{sync()} changes mode or commits a mature normal candidate, the wrapper
\must return \code{SYNCED} successfully before parsing or validating the
caller's candidate.  The caller resubmits against the new version.  This rule is
necessary because a later Solidity revert would otherwise roll back the sync
transition in the same transaction.

\section{Builder registry and exact lookahead}\label{sec:schedule}

\subsection{Registry lifecycle}

A registration contains a nonzero address, a \code{uint192} bond not exceeding
\code{BOND_MAX}, a monotonic \code{uint64 registrationIndex}, and separately
reserved \code{L_LEASE[window]} tranches.  Address zero is permanently reserved
for \code{VACANT}.  Duplicate live addresses and re-registration of a
tombstoned key are forbidden.

The active table has exactly 64 indexed cells.  Admission into a full table
atomically replaces only its minimum-bond live entry after the delayed-entry
rule; displaced entries retain historical liabilities.  Because the complete
table is bounded and authenticated, snapshot verification proves completeness
rather than accepting an unverifiable claimed ``top 64.''  Every registry
mutation updates a fixed-depth Keccak \code{registryRoot} over all 64 cells.

Eligibility is evaluated before ranking: an entry is eligible for window $W$
only if its registration was effective in the authenticated snapshot, it had a
fully reserved tranche for $W$, and its tombstone was not effective at that
snapshot.  The 64 eligible entries with greatest bond, then smallest
registration index, form the snapshot set.  If none exist, every ticket is
\code{VACANT}.  A later slash tombstones the key and increments
\code{admissionVersion}.  An open normal settlement window freezes the prior
version, so a slash cannot invalidate its accepted best; the new tombstone
applies to later normal windows and every recovery round.  Client lookahead
renders tickets at or after the effective slot as \code{VACANT}, while clearly
marking the frozen-window exception.  The key is excluded from later snapshots.

A tranche follows the finite state machine
\code{FREE -> RESERVED(W) -> LIABLE(W) -> RELEASED} or
\code{RESERVED/LIABLE -> SLASHED}.  Candidate acceptance marks every used
window liable.  Release requires the window to be below
\code{minAdmissibleSlot}, its normal/recovery and L1 reorg horizons to have
passed, and the evidence delay to have expired.  A bitmap per snapshot and a
ring of at most \code{MAX_LIVE_WINDOWS} replace unbounded slash-history arrays.

\subsection{Authenticated snapshot}

For aligned window $W$, let its start timestamp be
\code{GENESIS_TIMESTAMP+384W}.  The target is the greatest
\code{BEACON_GENESIS_TIME+12k} not later than eight L1 epochs before that
start; L2-genesis alignment is irrelevant.  Define
\code{sealDeadline(W)=windowStart-H_LOOK}.  A seal is valid only when its source
has \code{F_FINAL} depth and \code{block.timestamp < sealDeadline(W)}.  At or
after the deadline an unsealed window is permanently all-\code{VACANT}; a late
transaction cannot change it.  The contract performs this procedure:

\begin{enumerate}
\item For $j=1,\ldots,64$, query EIP-4788 at
\code{q=targetTimestamp+12j}; missing timestamps revert and are skipped.  The
first successful query identifies the \emph{carrier} execution block.  EIP-4788
returns its parent beacon-block root, not the carrier root.
\item Supply the carrier execution header.  EIP-2935 authenticates its hash;
the header timestamp equals $q$ and its
\code{parent_beacon_block_root} equals the EIP-4788 result.  Fork-specific SSZ
branches in that parent authenticate its slot and one execution payload's
\code{blockHash}, \code{stateRoot} and \code{prevRandao}.  The parent slot must
be at or before target, its payload timestamp must equal its beacon-slot time,
and its payload block hash must equal the carrier header's parent hash.  No
carrier, a later parent, or a fork/gindex mismatch means all \code{VACANT}.
\item Verify Merkle--Patricia storage proofs from that execution state root for
the registry header and \code{registryRoot}.  The caller supplies all 64
canonical serialized cells, including empty cells; recompute their six-level
Keccak tree and require equality to \code{registryRoot}.  Filter eligibility and
order the entries, then store the entry-root and seed.  The fixed leaf count
proves completeness with one MPT path rather than 64 independent storage paths.
The source payload and carrier have both reached \code{F_FINAL} depth.
\end{enumerate}

\code{sealWindow} is permissionless.  An optional maintenance distributor may
pay its event in a later transaction.  A missing seal fails closed to
\code{VACANT}; it cannot give an address an unauthorized ticket.  The snapshot
age plus 64-slot carrier scan, finality and seal margin fits the eight-epoch
geometry and remains far below EIP-4788's 8,191-slot history window.  Consensus
constants include the Deneb SSZ generalized indices; an L1 fork that changes
them requires a protocol-version upgrade, never runtime guessing.

For Deneb, Electra and Fulu, the generalized index from \code{BeaconBlock} root
to \code{slot} is 8 and to \code{body.execution_payload} is 201.  Within the
composed tree, payload \code{state_root}, \code{prev_randao}, \code{timestamp}
and \code{block_hash} are 6,434, 6,437, 6,441 and 6,444 respectively.  The seal
input names the L1 fork digest; only these configured digests are accepted.
Gloas or any fork that changes the container requires new constants, fixtures
and a protocol-version upgrade before its activation.

\subsection{Consensus byte encoding and allocation}

All hashes use Ethereum legacy Keccak-256.  Concatenation below is packed fixed
width; strings are the literal ASCII bytes without a length prefix:

\begin{align*}
seed_W &= H(\texttt{"slot-chain-seed-v1"} \| u256(chainId) \\
       &\quad \| u256(protocolVersion) \| u256(W) \| bytes32(prevRandao)),\\
qTie_i &= H(\texttt{"slot-chain-quota-tie-v1"} \| seed_W \| u64(regIndex_i)),\\
sTie_{p,i} &= H(\texttt{"slot-chain-slot-order-v1"} \| seed_W
                   \| u16(p) \| address20(i)).
\end{align*}

Starting from zero, capped D'Hondt awards each of 384 tickets to the unsaturated
entry maximizing \code{bond/(allocation+1)}, with
\code{Q_MAX=76}.  Quotients are compared by cross multiplication; a
\code{uint192} bond times \code{Q_MAX+1} cannot overflow \code{uint256}.  Equal
quotients use smallest \code{qTie}.  Remaining tickets are \code{VACANT}; six
addresses are required to fill a window.

Placement processes positions in order.  A real address is feasible if it has
a ticket remaining, differs from the preceding real address, and removing it
preserves \code{remaining[a] <= otherRemaining + 1} for every real address.
Choose the feasible value with smallest \code{sTie}; \code{VACANT} may repeat.
The executable model and its golden vectors are normative test fixtures.

\section{Blocks, promises and proof statement}\label{sec:blocks}

\subsection{Signed header}

A builder signs exactly:

\begin{verbatim}
(chainId, protocolVersion, verifyingContract, slot, parentHash,
blockHash, stateRoot, bodyRoot, anchorNumber, anchorHash,
forceRoot, forceCutoff, messageStart, messageEnd, dataManifestRoot,
 coinbase, tier, admissionVersion, episode, recoveryRevision, recoveryId)
\end{verbatim}

The normative EIP-712 type string is:
\begin{verbatim}
SlotChainBlock(
 uint256 chainId,uint256 protocolVersion,address verifyingContract,
 uint64 slot,bytes32 parentHash,bytes32 blockHash,bytes32 stateRoot,
 bytes32 bodyRoot,uint64 anchorNumber,bytes32 anchorHash,bytes32 forceRoot,
 uint64 forceCutoff,uint64 messageStart,uint64 messageEnd,
 bytes32 dataManifestRoot,address coinbase,uint8 tier,
 uint64 admissionVersion,uint64 episode,uint64 recoveryRevision,
 bytes32 recoveryId)
\end{verbatim}
The hashed ASCII has one space between each field type and field name and no
other whitespace; displayed line breaks, indentation and spaces after commas
are typography only.  The exact single-line literal and a standalone Keccak
implementation are in \code{commitment-model.py}.
\code{TYPEHASH} is legacy Keccak-256 of those ASCII bytes and equals
the concatenation of these two fragments:
\begin{center}
\code{0x0a0bfc3b8ba52a166f662055d0989146}\\
\code{5d916aa486808e78105890e98d65777f}
\end{center}
The EIP-712
domain is \code{("SlotChain", "2", chainId, verifyingContract)}.  Signatures
use low-$s$ secp256k1 and reject zero/recovered-zero addresses.  The execution
\code{blockHash} excludes the off-chain builder signature, avoiding a circular
hash definition.

\subsection{Three validity tiers}

\begin{enumerate}
\item \textbf{Normal signed.} Every block is signed by its scheduled builder
under the normal window's frozen \code{admissionVersion}.  Slots strictly
increase, each slot is no later than \code{currentL2Slot + CLOCK_SKEW}, and the
first slot strictly exceeds the canonical base slot.  Parent gaps are at most
\code{G_MAX=64}.
\item \textbf{Recovery signed.} The first block extends the L1-final canonical
tuple, binds the active episode and current recovery revision, and may cross an
unbounded time gap.  Every block still requires its scheduled builder signature
under the recovery round's current admission version.
\item \textbf{Escape unsigned.} Exactly one deterministic block extends the
L1-final canonical tuple during recovery.  It has no builder signature, no
beneficiary-controlled coinbase, no discretionary transaction and no blob
body.  It contains only the protocol anchor and the deterministic maximum
prefix of the activation-frozen forced queue.  When the queue is empty an
SLA-triggered episode may commit an empty anchor-only escape block.  Its slot is
\code{max(activationSlot + ESCAPE_OFFSET, canonical.tipSlot + 1)} and its anchor,
queue root/cutoff, block hash and state root are unique functions of the current
recovery round and proved execution result.
\end{enumerate}

Every candidate uses exactly one \code{batchAnchor}; every block repeats that
anchor and its authenticated \code{forceRoot/forceCutoff}.  The supplied RLP
execution header hashes to the EIP-2935 value, its timestamp is no later than
the first L2 block timestamp, and MPT proofs under its state root authenticate
the forced queue and any other historical contract state.  Requiring one
anchor, not up to 4,096 independent anchors, bounds both circuit and L1 work.
Cutoffs therefore do not change within a candidate; the first blocks drain the
frozen FIFO prefix and later blocks see an empty prefix.

The circuit verifies parent linkage, strict slot monotonicity, schedules,
signatures for tiers 1--2, version binding, anchor/header/MPT authentication,
forced-prefix ordering, execution, exact body/data coverage and public outputs.
It rejects more than 4,096 blocks, 12 schedule windows, 16 sealed sessions or
2,100 referenced blob records.

\subsection{Verifier statement ABI}

The L1 verifier accepts \code{verify(proof,uint256[2] digestLimbs)}.  Settlement
constructs the following Solidity struct in exactly this order and computes
\code{statementHash} as legacy Keccak of the ASCII domain
\code{"slot-chain-statement-v1"} followed by \code{abi.encode(S)}:
\begin{verbatim}
S = (uint256 chainId, uint256 protocolVersion,
     address verifyingContract,
     uint8 tier, bytes32 baseTupleHash, bytes32 endTupleHash,
     bytes32 candidateCommitment, uint64 blockCount,
     uint64 firstSlot, uint64 tipSlot,
     uint64 anchorNumber, bytes32 anchorHash,
     bytes32 anchorStateRoot, uint64 anchorTimestamp,
     bytes32 forceRoot, uint64 forceCutoff,
     uint64 startCursor, uint64 endCursor,
     uint64 admissionVersion,
     bytes32 admissionRoot,
     uint64 episode, uint64 recoveryRevision, bytes32 recoveryId,
     bytes32 scheduleRootsCommitment, uint8 scheduleWindowCount,
     bytes32 sessionRefsCommitment, uint8 sessionCount,
     uint16 dataRecordCount, bytes32 executionOutputsCommitment,
     address proofBeneficiary)
\end{verbatim}
\code{digestLimbs[0]} and \code{[1]} are respectively the high and low 128 bits
of \code{statementHash}; neither is reduced modulo the proof field.  The circuit
recombines them and proves the exact preimage.  Settlement independently checks
the base tuple, current/frozen versions and recovery round, recomputes the
bounded schedule-root and sealed-session-reference commitments from calldata,
and writes exactly \code{endTupleHash}.  \code{proofBeneficiary} is a proof
input, never builder-signed block data or coinbase.  Copying proof bytes cannot
redirect a later optional reward.

\subsection{Preconfirmation semantics}

A builder signature proves authorship and makes same-slot equivocation
slashable.  It does not prove that the body reached a prover or that the next
builder saw it.  A later scheduled builder may sign a profitable skip that the
protocol cannot distinguish from non-receipt.  Accordingly:

\begin{itemize}
\item before L1 candidate acceptance, a promise is \emph{observed};
\item after acceptance and before close, it is \emph{provisionally proved};
\item after normal close or recovery commit, it is \emph{canonical}; and
\item only the underlying L1 finality policy makes it \emph{L1-final}.
\end{itemize}

Wallets and applications \mustnot display the first two states as final.  An
escape commit explicitly revokes every omitted observed or provisionally proved
promise and returns omitted transactions to the mempool.

\section{Blob data authentication}\label{sec:data}

Data is posted into publisher-owned, bonded sessions.  A session ID is
\begin{verbatim}
H("slot-chain-session-v1" || u256(chainId) || address20(contract) ||
  address20(owner) || u64(ownerNonce))
\end{verbatim}
At most two sessions per owner and 1,024
globally may be live; each contains at most 2,100 records.  Opening uses the
first free ring cell, after deterministically evicting expired cells in index
order.  A full live ring rejects discretionary publication but cannot block the
forced/escape lane.  Rent covers worst-case garbage collection.

Only the owner may append, and a candidate may reference only a session made
immutable by \code{sealSession}.  Sealing fixes \code{(root,count,expiry)};
there is no unseal or append-after-seal operation.  A candidate supplies at most
16 sorted unique \code{(sessionId,count,root)} references.  At submission each
must equal a live sealed session.  Normal acceptance requires
\code{expiry >= normalDeadline + REORG_MARGIN_SECONDS}; recovery requires
\code{expiry >= block.timestamp + REORG_MARGIN_SECONDS}.  Opening requires
its expiry to cover proving, settlement and the reorg margin:
\begin{align*}
expiry &\ge now+P\_PROVE\_MAX+W\_SETTLE\_SECONDS\\
       &\quad+REORG\_MARGIN\_SECONDS,\\
expiry &\le now+DATA\_TTL.
\end{align*}

For every blob attached to \code{postData(session, manifests[])}, the contract:

\begin{enumerate}
\item obtains \code{versionedHash=BLOBHASH(i)} from the same transaction;
\item computes legacy Keccak over the packed fixed-width sequence
\code{"slot-chain-data-fs-v1",u256(chainId),u256(protocolVersion),
bytes32(sessionId),bytes32(versionedHash),bytes32(bodyRoot),
address20(publisher),u64(validUntil)}, interprets it big-endian and reduces it
modulo the BLS scalar-field modulus to obtain $z$;
\item calls the EIP-4844 point-evaluation precompile with exactly
\code{versionedHash32 || z32 || y32 || commitment48 || proof48}; and
\item appends the exact leaf from \S\ref{sec:encodings} to the session MMR.
\end{enumerate}

The proof receives blob polynomials privately, recomputes $P(z)=y$, decodes the
canonical blob codec and recomputes \code{bodyRoot}.  A body byte string is
\code{u32(txCount) || (u32(txLength)||signedRawTx)*}; its root is
\code{H("slot-chain-body-v1" || u32(byteLength) || bodyBytes)}.  Blob payload is
\code{u32(byteLength)||bodyBytes||zeroPadding}, split into 31-byte chunks; each
blob field element is \code{0x00||chunk31}.  Exactly 4,096 elements are used,
unused payload bytes are zero, and noncanonical encodings reject.  A manifest
uses increasing unique indices and partitions every discretionary body byte
exactly once.  Duplicate, extra or uncovered data is invalid.

Data publication has no consensus-coupled reimbursement.  A separate service
may pay it, but empty payment state cannot revert canonical settlement.

\section{Settlement and recovery state machine}\label{sec:machine}

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[node distance=20mm and 20mm]
\node[shaded,text width=30mm] (idle) {NORMAL\_IDLE};
\node[box,text width=34mm,right=of idle] (open) {NORMAL\_OPEN\\best + deadline};
\node[shaded,text width=34mm,below=of idle] (recovery) {RECOVERY\_OPEN\\episode + round};
\draw[arrow] (idle) to[bend left=23]
  node[above,font=\scriptsize]{first valid proof} (open);
\draw[arrow] (open) edge[loop right] node[right,font=\scriptsize]{heavier proof} (open);
\draw[arrow] (open) to[bend left=23]
  node[below,font=\scriptsize]{mature: commit best} (idle);
\draw[arrow] (idle) -- node[right,font=\scriptsize]{SLA / force: activate} (recovery);
\draw[arrow] (open) -- node[sloped,above,font=\scriptsize]{cancel immature} (recovery);
\draw[arrow] (recovery) to[bend left=52]
  node[left,font=\scriptsize]{valid tier 2/3} (idle);
\end{tikzpicture}
\caption{The complete mode machine.  Payments are outside every arrow.}
\end{figure}

\subsection{Normal candidates}

The first valid tier-1 candidate extending the canonical tuple opens a normal
window.  It freezes \code{baseCanonical},
\code{deadline = block.timestamp + W_SETTLE_SECONDS}, and
\code{minAdmissibleSlot = max(0,currentL2Slot - DELTA_TIP)}, and current
\code{admissionVersion}.  Before accepting the first candidate, Settlement
computes the prospective deadline; every candidate must consume all FIFO
messages whose \code{dueAt <= deadline}.  Later candidates use the same base,
deadline, minimum and admission version.  Their total order is lexicographic:

\[
(\text{block count descending},\ \text{tip slot descending},\
 \text{tip hash ascending}).
\]

Only a strictly better candidate replaces \code{normalBest}; provisional
replacement changes no canonical state and pays nobody.  At or after the
deadline, \code{sync()} commits the stored best only if its end cursor still
covers the required prefix.  Because \code{FORCE_DELAY >= W_SETTLE_SECONDS +
T_INCLUDE_MAX}, a message appended after the window opens cannot newly become
due before that deadline.  Settlement then recomputes recovery causes against
the new tuple.

\subsection{Activation}

While normal, \code{sync()} opens one persistent recovery episode if either:

\begin{itemize}
\item \code{currentL2Slot > canonical.tipSlot + DELTA_FINAL_LAG}; or
\item the forced queue head satisfies
\code{enqueuedAt + FORCE_DELAY <= block.timestamp}.
\end{itemize}

A due forced head has precedence over an immature normal window, which is
canceled.  At a mature deadline, a best that consumes the required due prefix
commits before causes are recomputed; an omitting best is canceled.  No best is
promoted across activation.  The contract increments \code{episode}, records
causes and the base-tuple hash, performs the SLA seat termination/burn at most
once, then opens recovery round one.

Every round freezes \code{roundStartSlot}, the preceding L1 block number/hash,
its timestamp, current \code{forceRoot/forceCutoff}, current admission version,
and
\code{escapeSlot=max(roundStartSlot+ESCAPE_OFFSET,canonical.tipSlot+1)}.
Its \code{expiresAt} is \code{GENESIS_TIMESTAMP+escapeSlot+DELTA_TIP}.  The exact
ID is:
\begin{verbatim}
recoveryId = H("slot-chain-recovery-v2", u256(chainId), address20(contract),
               u64(episode), u64(revision), bytes32(baseTupleHash),
               u64(roundStartSlot), u64(anchorNumber), bytes32(anchorHash),
               bytes32(forceRoot), u64(forceCutoff), u64(admissionVersion),
               bytes32(admissionRoot), u64(escapeSlot), u8(causes))
\end{verbatim}
At \code{block.timestamp > expiresAt}, and never before, \code{sync()} replaces
the expired target with revision $r+1$ and fresh round fields, then returns
\code{SYNCED}.  It does not change episode/base/cause, re-burn, terminate,
promote or pay.  The old proof is already inadmissible, so permissionless
refresh cannot grief a still-live proof.  New queue appends enter the refreshed
cutoff.  An envelope admitted during recovery receives
\code{dueAt=max(enqueuedAt+FORCE_DELAY,currentRound.expiresAt+1)}; therefore it
cannot become due while a frozen round that omits it remains admissible.

\subsection{Recovery acceptance}

Recovery is first-valid, not heaviest.  A tier-2 or tier-3 candidate \must:

\begin{enumerate}
\item extend the exact current stored canonical tuple;
\item bind current \code{episode}, \code{revision}, \code{recoveryId}, frozen
admission version, anchor and queue target;
\item land only after \code{block.number-anchorNumber >= F_L1}, have first slot
at least \code{roundStartSlot}, tip no older than
\code{DELTA_TIP}, and no slot later than wall clock plus skew;
\item consume the exact maximum forced-message prefix from the current cursor,
bounded by the activation-frozen cutoff; and
\item satisfy its tier-specific signature/data restrictions and validity proof.
\end{enumerate}

The first valid L1 transaction atomically commits the tuple, records the current
L1 block \emph{number only}, clears the recovery episode and returns to normal.
Later proofs are stale.  Candidate rejection cannot undo activation because of
the early-return wrapper in \S\ref{sec:state}.

\section{Forced queue and unsigned escape}\label{sec:escape}

A forced envelope is \code{(kind,sender,nonce,chainId,rawTx,byteLength,
accountedGas,maxFee,validUntil,refundAddress,enqueuedAt,dueAt,deposit)}.
For \code{kind=0}, admission canonically decodes the EIP-2718 signed raw
transaction and requires its recovered sender, nonce, chain ID, gas limit and
fee cap to equal the outer fields.  For \code{kind=1}, only the configured L1
BridgeAdapter may enqueue a canonical deposit/system call in the same
transaction that accepts the L1 bridge message; the L2 inbox transition is
idempotent, has no external call and is proved never to revert.  Other kinds
reject.  Both paths compute intrinsic/accounted gas and require
\code{accountedGas=max(tx.gasLimit,intrinsicGas,MIN_FORCE_ACCOUNTED_GAS)},
\code{0 < accountedGas <= 5,000,000}, \code{byteLength=rawTx.length <= 131,072},
a nonexpired \code{validUntil}, and a deposit covering execution, proof credit
and permanent storage.  Bytes are published in the enqueue transaction's L1
calldata and their Keccak is in the queue leaf.  Blobs are forbidden for this
availability path; reconstruction assumes access to canonical archival L1
calldata, the same assumption used by bridge indexers.

Each proved block consumes the unique maximum FIFO prefix satisfying all three
bounds simultaneously:
\[
count\le64,\qquad \sum byteLength\le1{,}048{,}576,\qquad
\sum accountedGas\le20{,}000{,}000.
\]
Every admitted item individually fits all three.  The circuit exposes
\code{(start,end,count,totalBytes,totalAccountedGas,forceRoot,forceCutoff)} and
proves no skip or early stop.  Launch enforces
\code{ANCHOR_GAS_MAX + FORCE_GAS_BUDGET + SYSTEM_GAS_MARGIN <=
L2_BLOCK_GAS_LIMIT} and the analogous witness-byte bound.

Every L2 body orders the protocol anchor transaction first, that block's forced
prefix contiguously second, and discretionary transactions last.  At execution,
an expired envelope, bad current nonce, insufficient upfront balance or
currently inadequate fee cap is consumed with a deterministic discard code and
has no Ethereum nonce/receipt effect.  An upfront-valid kind-0 transaction executes as
ordinary Ethereum: success or revert both increment nonce, charge gas and emit
a receipt.  A revert is \emph{included}, never mislabeled discarded.  Kind-1
executes its total system transition and cannot use discard semantics.  Unused
deposit is credited to \code{refundAddress} by a non-consensus module; failure
there cannot change queue consumption.

For a signed block, \code{forceRoot/forceCutoff} are the queue state authenticated
by that block's L1 anchor, and the circuit consumes the maximum prefix only up
to that cutoff.  A later L1 append therefore cannot invalidate an already signed
block or in-flight proof.  All blocks in one candidate use the same cutoff.  A
due current head cannot be bypassed: \code{sync()} cancels an immature window,
or closes a mature best only if it consumed the deadline-required prefix,
before opening recovery.

The 1,500-second force delay applies to the queue head, not to an arbitrary
position behind existing prepaid work.  An admitted sender can compute its
worst-case number of escape blocks from the gas ahead at admission.  As with L1
itself, the protocol does not promise backlog-independent latency against an
adversary willing to buy all available capacity; it promises that builders
cannot selectively veto the FIFO head.

An escape block is deterministic.  Its timestamp is its frozen slot, coinbase is the
protocol sink, base fee follows the ordinary formula, randomness is derived
from the frozen authenticated anchor, and its only transactions are anchor plus
the frozen forced prefix.  The circuit proves those facts.  No party gains
timestamp, ordering or MEV discretion by producing the proof.

For a cartel with no usable builder block, the recovery sequence is:

\begin{center}
\begin{tikzpicture}[node distance=9mm]
\node[box] (due) {SLA breach or message due};
\node[box,right=of due] (sync) {anyone includes \code{sync}};
\node[box,right=of sync] (depth) {round anchor reaches \code{F_L1}};
\node[box,below=of depth] (prove) {anyone proves deterministic escape};
\node[shaded,left=of prove] (commit) {escape commits};
\draw[arrow] (due)--(sync); \draw[arrow] (sync)--(depth);
\draw[arrow] (depth)--(prove); \draw[arrow] (prove)--(commit);
\end{tikzpicture}
\end{center}

With the initial bounds, activation inclusion (120 s), the frozen
\code{ESCAPE_OFFSET=1,900 s} (covering \code{T_DEPTH_MAX=900 s}, proving at
900 s and 100 s margin), submission inclusion (120 s), and clock margin (24 s)
total 2,164 seconds, below \code{DELTA_FINAL_LAG=3,600 s}.  Tip admissibility is
separate: the escape slot is at the end of the offset, so only submission and
skew---144 seconds---must fit \code{DELTA_TIP=1,200 s} when proof generation
meets its bound.

\section{Economics, slashing and payments}\label{sec:economics}

\begin{xltabular}{\linewidth}{p{0.25\linewidth}p{0.25\linewidth}X}
\toprule
\textbf{Event} & \textbf{Objective evidence} & \textbf{Effect} \\
\midrule
Same-slot equivocation & Two valid distinct signed headers for one key/slot &
Slash that schedule window's tranche once; tombstone key; emit reporter entitlement. \\
Aggregator SLA breach & Final lag exceeds threshold after mature close &
Terminate and burn penalty bond once; open recovery; promote standby if any. \\
Force-only activation & Due queue head & No aggregator penalty; open recovery. \\
Skip or absence & None that distinguishes fault from non-receipt &
Not slashable; promise may be revoked. \\
Invalid proof/data & Verifier failure & Reject; no canonical or payment effect. \\
\bottomrule
\end{xltabular}

Equivocation evidence is idempotent per \code{(builder, window)}; separate
windows slash separate tranches.  \code{L_LEASE} is calibrated against at most
76 assigned slots but is not insurance for unbounded signed value.  Tombstoning
is atomic with the first valid slash; future snapshots exclude the key.

Canonical settlement performs no reward calculation, balance write or external
call.  Its event records \code{candidateId} and the proof-bound beneficiary.
In a separate transaction an optional \code{RewardDistributor} checks that ID
against Settlement and materializes at most the funded cap from forced deposits,
the terminated seat's recovery escrow and an optional reserve.  Failure or
exhaustion affects only that second transaction.  Burns are never recycled into
the same episode's bounty.  Normal service/data compensation likewise cannot
affect candidate validity or canonical commit.

\section{Security and liveness analysis}\label{sec:analysis}

\begin{xltabular}{\linewidth}{p{0.23\linewidth}p{0.34\linewidth}X}
\toprule
\textbf{Adversary/failure} & \textbf{Protocol result} & \textbf{Bound or residual} \\
\midrule
Invalid execution or forged state & Proof rejects. & Finalized-state safety reduces to proof-system and L1 safety. \\
Aggregator offline/byzantine & SLA activation terminates it; any prover uses tier 2 or 3. & At most final-lag threshold plus 2,164 s under stated inclusion/proof bounds. \\
No seat or no standby & State remains valid; escape has no seat dependency. & Same protocol bound; reward may be zero. \\
All builders collude/absent & Unsigned escape advances anchor and forced queue. & Meaningful discretionary throughput falls to forced envelopes; no builder censorship veto. \\
Builder equivocation & Competing signed branches; one may win normal order. & Direct per-window slash; pre-close promises remain revocable. \\
Builder skips/withholds body & Honest tail may be unprovable or omitted. & Not objectively slashable; eventual escape restores state progress but may revoke promises. \\
Data-session spam & Sessions are publisher-owned and bonded. & Attacker fills only its session; escape uses no blob session. \\
Transparent-mempool front-run & First valid recovery transaction wins. & Proof statement binds beneficiary, episode, revision and outputs; copying cannot redirect payment or state. \\
Payment-vault exhaustion & Claim is zero or partial. & Never blocks safety or canonical liveness; economic liveness is conditional. \\
L1 reorg within policy & Contract state, burns and claims roll back together. & Clients replay canonical logs and proofs against new version/hash. \\
L1 censorship/outage & Neither activation nor proof can land. & Outside model after \code{T_INCLUDE_MAX}; no L2 protocol can force its L1 contract to execute. \\
\bottomrule
\end{xltabular}

The escape lane establishes strong transaction-entry permissionlessness even
when the capital-ranked builder set is captured.  It does not make the normal
builder market egalitarian: top-64 ranking and per-window capital remain a
meaningful entry barrier.  The product must distinguish these two claims.

\section{Implementation blueprint}\label{sec:implementation}

\subsection{L1 modules and bounded storage}

\begin{itemize}
\item \code{BuilderRegistry}: entries, tranche bitmap, tombstone and bounded
liability ring.
\item \code{ScheduleOracle}: sealed window root/seed in a ring covering
\code{MAX_LIVE_WINDOWS=262}.  In window units this is at least
the ceiling of lookahead, candidate, settlement, recovery, evidence and reorg
horizons:
\begin{verbatim}
ceil(H_LOOK/384) + MAX_CANDIDATE_WINDOWS +
ceil((W_SETTLE_SECONDS + DELTA_FINAL_LAG + DELTA_TIP +
      REORG_MARGIN_SECONDS + EVIDENCE_DELAY_SECONDS) / 384) + 2
\end{verbatim}
An entry is overwritten
only if its release predicate is true and no stored normal best references it.
\item \code{DataSession}: a 1,024-cell global ring, at most two live cells per
owner, each with owner/nonce ID, 2,100-leaf MMR frontier, sealed root/count and
expiry; no on-chain leaf array.  Expired eviction is deterministic.
\item \code{ForcedQueue}: append-only envelope commitment, cursor and deposits;
payload bytes are permanent L1 calldata and never blobs.
\item \code{Settlement}: PREACTIVE flag, canonical tuple, one normal best,
recovery episode/round and seat pointer.  Rewards are separate.  No loop is
proportional to chain age.
\end{itemize}

All setters enforce parameter inequalities atomically and are disabled after
the launch freeze except through the existing delayed governance path.  Every
counter has an explicit width and checked increment; no sentinel shares a legal
value.

\subsection{Historical authentication}

EIP-2935 authenticates normal candidate and schedule carrier headers.  A
recovery round captures its immediately preceding block hash with
\code{blockhash(block.number-1)} and later compares candidates with that stored
value; it never queries an expired history entry.  EIP-4788 plus SSZ branches
authenticates the schedule's parent beacon/execution payload; its state root
plus MPT proofs authenticates the registry.  Deployments lacking either
primitive require an equivalently verified oracle and separate review.

\subsection{Reorg rule}

The canonical L1 log is the journal.  A reorg truncates and replays, in order:
canonical tuple, mode/version, normal best/deadline/frozen admission, episode/round,
seat termination/burn, tombstone/tranche state, forced cursor, data-session
root/count, and reward eligibility.  Off-chain workers index by
\code{(chainId, contract, blockHash, logIndex)} and discard orphaned jobs.
Withdrawal/bridge execution waits for the configured L1 finality policy.

\subsection{Genesis and migration}

Deployment occurs at least \code{D_SNAP + F_FINAL + sealMargin} before
activation, so first live sources are post-deployment and sealable.  New
Settlement begins \code{PREACTIVE}; registry, scheduling and data preparation
may run, but every candidate rejects.

Forced user transactions and kind-1 bridge messages live in one shared
\code{ForcedQueue} used by old and new settlement contracts, so there is no
queue-root/cursor copy race.  Routing the legacy bridge through the audited
BridgeAdapter is a migration prerequisite; deployment is blocked if any old
pending message is not represented exactly once in this queue.  At or after
the announced activation time, one governance executor transaction calls
\code{old.freezeAndExport(new)}.  The old contract first performs its objective
sync, cancels any remaining provisional window, permanently disables candidate
ingress and exports its exact current canonical tuple, forced cursor and bridge
withdrawal cursor to the new contract.  The new contract checks the shared
queue root/cursor, imports those values and changes PREACTIVE to NORMAL before
the same transaction returns.  Any revert rolls both contracts back.  Bridge
claims are gated by the exported withdrawal cursor/root.  Thus no L1 ordering
interval can advance the old state between snapshot and import, and no slot or
message is accepted by both or neither verifier.

\section{Parameters and enforced geometry}\label{sec:parameters}

\begin{xltabular}{\linewidth}{p{0.31\linewidth}p{0.19\linewidth}X}
\toprule
\textbf{Parameter} & \textbf{Initial value} & \textbf{Enforced relation} \\
\midrule
L2 slot / schedule window & 1 s / 384 slots & Aligned by genesis timestamp. \\
\code{N_MAX}, \code{Q_MAX} & 64 / 76 & Six addresses fill 384 tickets. \\
\code{BOND_MAX} & $2^{192}-1$ & \code{bond*(Q_MAX+1) < 2^256}. \\
\code{D_SNAP}, \code{F_FINAL} & 8 / 2 L1 epochs & Strictly exceeds scan + finality + seal margin + lookahead. \\
\code{H_LOOK} & 768 L2 slots & Guaranteed by snapshot geometry with one-window slack. \\
\code{G_MAX} & 64 L2 slots & Tier-1 parent gap only. \\
\code{W_SETTLE_SECONDS} & 1,200 s & Timestamp deadline, never L1 block count. \\
\code{DELTA_TIP} & 1,200 s & At least submit inclusion + skew = 144 s after the frozen escape slot. \\
\code{DELTA_FINAL_LAG} & 3,600 s & At least activation + escape offset + submit + skew = 2,164 s. \\
\code{F_L1}; \code{T_DEPTH_MAX} & 64 L1 blocks; 900 s & Stored recovery-anchor depth and assumed maximum time to reach it. \\
\code{ESCAPE_OFFSET} & 1,900 s & At least depth-time bound + proof bound + margin. \\
\code{FORCE_DELAY} & 1,500 s & At least settlement window plus 120-s inclusion bound. \\
Forced prefix & 64 items; 1,048,576 B; 20m gas & Each item at most 131,072 B and 5m accounted gas. \\
Candidate caps & 4,096 blocks; 12 windows; 1 anchor; 16 sessions; 2,100 records & Independent verifier/circuit limits. \\
\code{DATA_TTL} & 86,400 s & Greater than candidate, settlement, proof and reorg path. \\
\code{REORG_MARGIN}, \code{EVIDENCE_DELAY} & 1,800 / 86,400 s & Data resubmission and finite liability retention. \\
Live windows / sessions & 262 / 1,024 & Fixed rings with safe-eviction predicates. \\
EIP history & 8,191 entries & Greater than every normal-anchor and schedule path. \\
\bottomrule
\end{xltabular}

Launch tooling evaluates every inequality in both seconds and its native clock
unit.  Governance cannot set a value that violates a relation.

\section{Production gates and verdict}\label{sec:gates}

The consensus design is fixed enough to implement.  Production deployment is
blocked until every gate below produces a versioned artifact and passes:

\begin{enumerate}
\item \textbf{Proof performance.} Independent tier-1, tier-2 and worst-case
tier-3 proofs, including 4,096 blocks and 2,100 data records, finish within 900
seconds on the published minimum hardware.  Peak RAM, recursion setup and
failure recovery are measured; a miss requires changing parameters and another
design review.
\item \textbf{Contract gas.} \code{sealWindow}, \code{postData}, normal submit,
\code{sync}, recovery submit, slash and garbage collection fit current L1 gas
limits with 30\% margin.  Fuzzing includes empty registry, zero seat, full data
session, full queue prefix and maximum history age.
\item \textbf{Cryptographic conformance.} Solidity, Go, Rust and circuit code
match the Keccak/EIP-712 golden vectors, SSZ generalized indices, MPT proofs,
KZG Fiat--Shamir transcript and exact body manifest.  Two independent
implementations generate every vector.
\item \textbf{State-machine verification.} Stateful fuzzing models EVM rollback,
same-block ordering, reorg replay, stale versions, activation/close coincidence,
message consume-and-discard and storage reclamation.  The executable models in
Appendix~\ref{app:models} remain green.
\item \textbf{Economics.} Auction bond, per-window tranche, forced-envelope
deposit, storage bond and proof credits are calibrated under blob/L1 fee spikes.
The published liveness claim explicitly becomes conditional above fee caps.
\item \textbf{Operations.} At least two independent prover stacks, public data
session mirrors, schedule sealers and recovery bots complete a shadow-fork
outage exercise.  The exercise removes every builder and seat, forces a user
transaction, reorgs the first recovery attempt, and still reaches finality.
\item \textbf{External review.} Separate proof-system, Solidity, bridge and
economic audits close all critical/high findings; the migration rehearsal and
emergency governance path are in scope.
\end{enumerate}

\begin{designnote}
\textbf{Verdict.} The earlier fallback architecture was not implementation-ready:
it depended on a scheduled builder, could revert activation inside a rejected
submission, made payment a commit precondition, and could not bind whole-window
data with a six-blob cap.  This revision removes those dependencies and fixes a
single consensus path.  It is implementation-ready as a specification.  It is
not production-ready as a deployed system until the seven empirical gates pass;
claiming otherwise from a document and Python assertions alone would be unsound.
\end{designnote}

\clearpage
\appendix
\section{Normative commitment encodings}\label{sec:encodings}

All integers in packed commitments are unsigned big-endian at the named width;
addresses are 20 bytes, hashes are 32 bytes, and ASCII domains have no length
prefix or trailing NUL.  \code{H} is Ethereum legacy Keccak-256.  No
\code{abi.encodePacked} call may contain a dynamic field.  Raw transaction bytes
are represented by \code{H(rawTx)} plus an explicit \code{u32(byteLength)}.

The canonical tuple, candidate, schedule list, sealed-session list and execution
outputs use these exact encodings:
\begin{verbatim}
H("slot-chain-canonical-v1" || tipHash || u64(tipSlot) || stateRoot ||
  u64(messageCursor) || u64(canonicalizationBlock) || winningDataCommitment)
H("slot-chain-candidate-v1" || baseTupleHash || u16(n) ||
  (u64(slot) || blockHash || bodyRoot || u64(messageEnd))*)
H("slot-chain-schedule-list-v1" || u8(count) ||
  (u64(window) || entryRoot || seed)*)
H("slot-chain-session-list-v1" || u8(count) ||
  (sessionId || u16(recordCount) || root)*)
H("slot-chain-outputs-v1" || stateRoot || transactionsRoot || receiptsRoot ||
  logsBloomHash || withdrawalsRoot)
\end{verbatim}
Here $n\le4096$; schedule windows are ascending and sessions are ascending by
ID.  These hashes are respectively the statement's
\code{baseTupleHash}, \code{candidateCommitment},
\code{scheduleRootsCommitment}, \code{sessionRefsCommitment} and
\code{executionOutputsCommitment}; unsorted or duplicate lists reject.

The 64-cell registry leaf at index $i$ is
\begin{verbatim}
H("slot-chain-registry-leaf-v1" || u8(i) || u8(present) ||
  address20 || u192(bond) || u64(registrationIndex) ||
  u64(effectiveSlot) || bytes32(trancheRoot) || u64(tombstoneSlot))
\end{verbatim}
An empty cell has \code{present=0} and every following byte zero.  A present
cell has \code{present=1}; \code{UINT64_MAX} means not tombstoned.  At tree
height $h=0,\ldots,5$, the parent is:
\begin{verbatim}
H("slot-chain-registry-node-v1" || u8(h) || left || right)
\end{verbatim}
There is no padding because there are exactly 64 leaves.

Each registry cell's tranche ring is a 512-leaf tree.  Leaf index $j$ is:
\begin{verbatim}
H("slot-chain-tranche-leaf-v1" || u16(j) || u64(window) || u8(state) ||
  u192(amount) || u64(liableUntil))
\end{verbatim}
An empty leaf has \code{window=UINT64_MAX},
state zero and remaining fields zero.  Its nine node levels use
\code{H("slot-chain-tranche-node-v1"||u8(height)||left||right)}.  The snapshot
supplies one inclusion proof for the requested window per eligible cell.

Forced leaf $i$ is legacy Keccak over this exact packed sequence:
\begin{verbatim}
"slot-chain-force-leaf-v1" || u64(i) || u8(kind) ||
address20(sender) || u64(nonce) || u256(chainId) || bytes32(rawTxHash) ||
u32(byteLength) || u64(accountedGas) ||
u256(maxFee) || u64(validUntil) || address20(refundAddress) ||
u64(enqueuedAt) || u64(dueAt) || u256(deposit)
\end{verbatim}
The empty queue root is \code{H("slot-chain-force-init-v1")}.  Append leaf $L_i$
as \code{root[i+1]=H("slot-chain-force-node-v1"||root[i]||L_i||u64(i+1))}.
The count is therefore bound at every step.  A queue proof supplies the prior
root and contiguous leaves; there is no membership/nonmembership format that
can skip an index.

A data leaf is:
\begin{verbatim}
H("slot-chain-data-leaf-v1" || bytes32(sessionId) || u16(index) ||
  bytes32(versionedHash) || bytes32(bodyRoot) || address20(publisher) ||
  u64(validUntil) || u256(z) || u256(y))
\end{verbatim}
MMR append merges equal-height peaks as
\code{H("slot-chain-data-node-v1"||u8(height)||left||right)}.  Peaks are stored
left-to-right in strictly decreasing height.  The root bags them
rightmost-to-leftmost as
\code{H("slot-chain-data-bag-v1"||u16(count)||u8(peakCount)||
(u8(height)||peak)*)}.  An inclusion proof provides the leaf index, merge
siblings bottom-up with a left/right bit at each height, then every other peak
in this bag order; duplicate heights, a count inconsistent with peak heights,
or unused proof elements reject.  The empty MMR is the bag hash with zero count
and zero peaks.

The recovery ID is the encoding in \S\ref{sec:machine}.  Schedule leaf/root,
seed and tie hashes are as in \S\ref{sec:schedule}; body and blob encodings are
as in \S\ref{sec:data}.  \code{commitment-model.py} is the normative executable
fixture.  Its initial vectors are:
\begin{verbatim}
TYPEHASH      0a0bfc3b8ba52a166f662055d09891465d916aa486808e78105890e98d65777f
registryRoot  0bf297d7b9b6a5529a319a06cb08484923a89bab15d51f8baeaf5c30bebdf3fd
forcedRoot    0e5416c12bf86837bad8fd0370f247683a401ceaa80f45e4a3fca52876ad448d
MMR(root,2)   f06fd107666ba9e51ad09606ba9c9f1f1c16177f586167f87b16f333806d485d
recoveryId    9970a429a00e56b8abc5e7fcb4a4a35795507e949821be66f2f737e888ded285
bodyRoot      0f4e161a46c8b18c2a86f23a0a4e7169a838a12af8b389f65e97b547a99707e9
\end{verbatim}
Release CI must reproduce every vector in Solidity, Go, Rust and the circuit.

\section{Normative transition ordering}\label{app:ordering}

Every external candidate entry point implements the following structure:

\begin{verbatim}
function submit(input) returns (Status) {
    if (mode == PREACTIVE) return REJECTED_PREACTIVE;
    if (sync()) return SYNCED;     // successful transaction, never revert
    if (mode == NORMAL) return submitNormal(input);
    return submitRecovery(input);
}

function sync() returns (bool changed) {
    if (mode == RECOVERY && now > round.expiresAt) {
        rollRound();               // same episode/base/burn; new target
        return true;
    }
    if (mode == NORMAL && forcedHeadDue && normal window immature) {
        cancel normal window;
    } else if (normal deadline matured) {
        if (best covers requiredCursor(deadline)) commit(normalBest);
        else cancel normal window;
    }
    if (mode == NORMAL && (finalLagBreach || forcedHeadDue)) {
        openRecoveryEpisodeAndRound();
        if (finalLagBreach) terminate incumbent once;
        return true;
    }
    return whetherCloseOrCancelChangedState;
}
\end{verbatim}

No transfer, reward calculation, unbounded loop or caller-controlled external
call occurs in canonical settlement.  Optional reward materialization is a
later transaction in a separate module.

\section{Rejected alternatives}\label{app:alternatives}

\begin{enumerate}
\item \textbf{Heaviest fallback.} Rejected because an observer cannot prove it
has seen the globally heaviest off-chain tail; transparent races can prevent a
stable target.  Recovery is episode-bound first-valid.
\item \textbf{Scheduled-builder-only fallback.} Rejected because the same cartel
that stalls normal production also controls recovery.  Tier 3 is deterministic
and unsigned.
\item \textbf{Promote the pre-activation normal best.} Rejected because it was
accepted under different cutoff/version semantics.  Only a mature best that
covers the required forced prefix closes; immature state is canceled.
\item \textbf{Atomic reward-funded commit.} Rejected because an empty seat or
vault becomes a consensus deadlock.  Reward materialization is a separate
non-authoritative transaction.
\item \textbf{One same-transaction blob set.} Rejected because Ethereum exposes
at most a handful of blobs per transaction while a candidate spans thousands of
blocks.  Publisher-owned authenticated sessions separate posting from proof.
\item \textbf{One KZG opening at a prover-chosen point.} Rejected because it does
not soundly tie declared bodies to the blob.  The challenge commits to both blob
hash and body root, and the circuit recomputes the evaluation and body root.
\item \textbf{Arithmetic snapshot slot without EIP-4788 parent semantics.}
Rejected.  A bounded forward carrier search authenticates the parent source or
fails closed.
\item \textbf{Bond as insurance.} Rejected without an on-chain reservation
ledger.  The tranche is deterrence only.
\end{enumerate}

\section{Executable consensus models}\label{app:models}

\code{lookahead-model.py} reports 29 assertions over legacy Keccak, EIP-4788
carrier/parent selection, immutable seal deadlines, post-seal tombstones,
eligibility-before-ranking, capped D'Hondt and feasible placement, including
fixed seed and complete-schedule vectors.

\code{settlement-window-model.py} reports 55 assertions over PREACTIVE/migration,
early-return semantics, force-due precedence, renewable recovery, no-seat
escape, triple-bounded prefixes, one-anchor chronology, sealed data sessions,
frozen admission epochs, replay and timing geometry.  Cryptographic booleans in
that model are not cryptographic evidence.

\code{commitment-model.py} reports 12 vectors/properties for the exact EIP-712,
registry, forced queue, session MMR, recovery ID, KZG transcript/precompile,
body and blob encodings.  The conformance and proof gates remain mandatory.

\section{Security invariants checklist}\label{app:invariants}

\begin{enumerate}
\item Canonical state changes only after one valid proof and only from its stored
base tuple.
\item A sync transition cannot be rolled back by validation of the same call's
candidate.
\item No payment, seat or standby is required for canonical commit.
\item Recovery candidates bind one live episode/revision, the current base
tuple, one stored prior-block anchor at required depth and one frozen queue
target.  Expired rounds are permissionlessly renewable without another burn.
\item A due forced head advances in the next canonical block; appends omitted
by a frozen recovery round cannot become due until that round expires.  Prefix
count, bytes and accounted gas are independently bounded.
\item Tier 3 has no builder signature, discretionary transaction, arbitrary
coinbase or blob body.
\item Every tier-1/2 block has a valid scheduled signature under the frozen
admission version, strict slot progress and the candidate's one authenticated
anchor no later than its L2 time.
\item Every discretionary body byte is covered exactly once by a unique,
unexpired authenticated data record.
\item Schedule inputs share one authenticated execution payload and use exact
Keccak encodings.
\item Tombstoned keys cannot re-enter; each window has an independent slashable
tranche and finite release state.
\item Candidate, window, data-session, queue count/bytes/gas, history and
storage-retention work is bounded by consensus constants and safe eviction.
\item L1 reorg replay removes canonical state, penalties and credits from
orphaned transactions together.
\item Migration freezes old ingress and imports canonical/bridge cursors into
PREACTIVE settlement atomically while using one shared forced queue.
\end{enumerate}

\end{document}
