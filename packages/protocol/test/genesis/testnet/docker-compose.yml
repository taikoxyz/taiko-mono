services:
  geth:
    image: ethereum/client-go:v1.17.2
    restart: unless-stopped
    volumes:
      - ../data:/data
      - .:/host
    ports:
      - 18545:8545
    entrypoint: "/host/init_geth.sh"
