name: Bridge UI CI/CD

on:
  push:
    paths:
      - "packages/bridge-ui/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - ".github/actions/install-pnpm-dependencies/**"
      - ".github/workflows/bridge-ui--ci.yml"
      - ".github/workflows/bridge-ui.yml"
    branches-ignore:
      - dependabot/**
      - release-please--branches--**

  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - "packages/bridge-ui/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - ".github/actions/install-pnpm-dependencies/**"
      - ".github/workflows/bridge-ui--ci.yml"
      - ".github/workflows/bridge-ui.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    if: ${{ github.event.pull_request.draft == false  && !startsWith(github.head_ref, 'release-please') && !startsWith(github.head_ref, 'dependabot') }}
    uses: ./.github/workflows/bridge-ui--ci.yml
