name: "Taiko Client Hive Tests"

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - "packages/taiko-client/**"
      - "go.mod"
      - "go.sum"
    branches-ignore:
      - release-please--branches--**

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  hive_tests:
    if: ${{ github.event.pull_request.draft == false && contains(github.event.pull_request.labels.*.name, 'option.hive-test') }}
    name: hive tests
    runs-on: [arc-runner-set]
    timeout-minutes: 40

    steps:
      - name: Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.12.1
        with:
          access_token: ${{ github.token }}

      - name: Install Git and Make
        run: sudo apt-get update && sudo apt-get install -y git make

      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 1.23
          cache: true

      - name: Clone taikoxyz/hive
        run: git clone --branch pacaya_fork https://github.com/taikoxyz/hive.git /tmp/hive

      - name: Build dependency and taiko images
        working-directory: /tmp/hive
        run: |
          make hive_alpine_dependency
          make hive_go_dependency
          make -C hiveproxy hiveproxy
          make -C simulators/taiko taiko

      - name: Hive tests
        working-directory: packages/taiko-client
        run: |
          export HIVE_DIR=/tmp/hive
          go test -v -p=1 ./integration_test -timeout=2400s
